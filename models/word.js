class Word {
    constructor(id, ownerId, title, type, definition) {
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.type = type;
        this.definition = definition;
    }
}

export default Word;