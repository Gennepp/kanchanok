class Word {
    constructor(id, ownerId, title, definition) {
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        //this.type = type;
        this.definition = definition;
    }
}

export default Word;